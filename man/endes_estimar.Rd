% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimacion.R
\name{endes_estimar}
\alias{endes_estimar}
\title{endes_estimar}
\usage{
endes_estimar(
  diseno,
  formula,
  por = NULL,
  fun = "proporcion",
  denominador = NULL,
  nivel = 0.95,
  na.rm = TRUE
)
}
\arguments{
\item{diseno}{Objeto \code{survey.design2}.}

\item{formula}{Formula que especifica la variable de resultado
(e.g., \code{~indicador}).}

\item{por}{Formula opcional para estimacion por dominio
(e.g., \code{~V025}).}

\item{fun}{Caracter. Tipo de estimacion: \code{"proporcion"} (default),
\code{"media"}, \code{"total"} o \code{"razon"}.}

\item{denominador}{Formula. Solo para \code{fun = "razon"}: formula del
denominador (e.g., \code{~total_hogares}).}

\item{nivel}{Numerico. Nivel de confianza para intervalos (default: 0.95).}

\item{na.rm}{Logico. Remover NAs antes de estimar (default: TRUE).}
}
\value{
Tibble con columnas: \code{variable}, \code{estimacion}, \code{se},
  \code{ci_inf}, \code{ci_sup}, \code{cv}, \code{n_sin_pond}. Si se usa
  \code{por}, incluye columna(s) de dominio.
}
\description{
Estimaciones ponderadas por diseno de encuesta: media,
  proporcion, total o razon, con opcion de estimacion por dominio.
}
\examples{
\dontrun{
diseno <- endes_diseno_encuesta(datos, unidad = "mujer")
endes_estimar(diseno, ~indicador, fun = "proporcion")
endes_estimar(diseno, ~indicador, por = ~V025, fun = "proporcion")
}

}
